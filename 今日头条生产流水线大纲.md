# 今日头条生产流水线大纲

## 一、项目定位

- 将小红书内容改编后发布到 **今日头条**
- 头条号后台：`mp.toutiao.com`
- 核心差异：头条支持长文 + 行内图片插入 + 原创声明

## 二、核心工具链

| 工具 | 用途 |
|------|------|
| **opencli** | 浏览器自动化发布头条 |
| **Python 3** | 内容改编（XHS → 头条） |

## 三、完整发布流程

### Step 1：环境准备
```bash
opencli bind toutiaohao
opencli doctor  # 确认浏览器扩展已连接
```

### Step 2：打开头条号后台
```bash
opencli browser <session> goto "https://mp.toutiao.com/profile_v4/graphic/publish-article"
```

### Step 3：填写标题
```bash
opencli browser <session> fill "[data-e2e='article-title-input']" "标题（≤30字）"
```

### Step 4：填写正文
```bash
opencli browser <session> fill "div[contenteditable=true]" "正文内容"
```
> 头条编辑器选择器与 XHS 不同，必须用 `div[contenteditable=true]`。

### Step 5：上传封面
```bash
opencli browser <session> upload "[data-e2e='cover-upload']" "./cover.png"
```
- 封面建议 2.35:1（1792×763）或 1:1（1024×1024）

### Step 6：插入行内图片
```bash
opencli browser <session> eval "
  const btn = document.querySelector('[data-e2e=\"insert-image\"]');
  btn.click();
  // 选择文件上传...
"
```
- 行内图建议 16:9 或 4:3
- ⚠️ 图片通过工具栏插入到**当前光标位置**，多次插入的图片全部在同一位置，需手动拖拽调整

### Step 7：勾选原创声明 + 发布
```bash
opencli browser <session> click "[data-e2e='original-toggle']"
opencli browser <session> click "[class*='publish-btn']"
```

### 备选：仅准备草稿
- 头条后台**导航离开会丢失全部内容**，务必一次性完成
- 草稿箱页面返回发布页也会丢失，需从头开始

## 四、内容改编规则

### XHS → 头条转换

| 维度 | 小红书 | 今日头条 |
|------|--------|----------|
| 标题 | ≤ 20 字，情绪化 | ≤ 30 字，可更信息密度 |
| 正文 | ≤ 950 字 | ≤ 2500 字 |
| 风格 | 口语化，像朋友聊天 | 偏正式，信息密度高 |
| 图片 | ≤ 9 张 | 封面 + 行内 3-6 张 |
| 话题 | `#话题` 标签 | 无话题标签，靠分类 |

### 爆款公式（头条）
- 标题：信息密度 > 情绪驱动
- 正文：观点 + 数据 + 案例
- 结构：引言 → 主体（3-5 段） → 结论

## 五、CSS 选择器速查

| 元素 | 选择器 |
|------|--------|
| 标题输入 | `[data-e2e='article-title-input']` |
| 正文编辑 | `div[contenteditable=true]` |
| 封面上传 | `[data-e2e='cover-upload']` |
| 行内图插入 | `[data-e2e='insert-image']` |
| 原创声明 | `[data-e2e='original-toggle']` |
| 发布按钮 | `[class*='publish-btn']` |

## 六、已知坑点

1. **草稿持久性**：导航离开发布页会丢失全部内容，务必一次性完成
2. **SPA 导航**：头条是单页应用，DOM 频繁重渲染，ref 编号会变
3. **发布按钮**：`[class*='publish-btn']` 需验证，后台可能改版
4. **封面图片**：上传后需等待缩略图加载完成再点击发布
5. **行内图片**：插入后光标位置不变，需手动调整
6. **声明勾选**：用 `eval` + `querySelector` 定位，不用 `contains()`（jQuery 语法）
7. **浏览器稳定性**：头条后台 React 渲染频繁，`opencli click` 可能 stale
8. **opencli 适配器**：`toutiaohao` 适配器状态需定期验证

## 七、完整操作示例（murongxi-xhs）

```bash
# 1. 绑定 + 打开后台
opencli bind toutiaohao
opencli browser <session> goto "https://mp.toutiao.com/profile_v4/graphic/publish-article"

# 2. 填标题
opencli browser <session> fill "[data-e2e='article-title-input']" "慕容熙：后赵暴君"

# 3. 填正文（从 article.md 提取）
BODY=$(sed -n '2,30p' article.md)
opencli browser <session> fill "div[contenteditable=true]" "$BODY"

# 4. 上传封面
opencli browser <session> upload "[data-e2e='cover-upload']" "./cover.png"

# 5. 插入行内图
for img in card-1.png card-2.png card-3.png; do
  opencli browser <session> upload "[data-e2e='insert-image']" "./$img"
done

# 6. 勾选原创 + 发布
opencli browser <session> click "[data-e2e='original-toggle']"
opencli browser <session> click "[class*='publish-btn']"
```

## 八、参考文献

- `小红书生产流水线大纲.md` — XHS 核心规范
- `MEMORY.md` — 案例记录 + 踩坑沉淀
- `murongxi-xhs/toutiao_inline.md` — 首次适配样本
