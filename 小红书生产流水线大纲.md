# 小红书生产流水线大纲

## 一、项目定位

- 社交媒体系列：Markdown 文章 + PNG 卡片，投放于 **小红书**
- 当前 ~100+ 个 `*-xhs/` 内容文件夹
- 三种内容类型：**旅行文化型**、**比赛事件型**、**知识科普型**

## 二、核心工具链

| 工具 | 用途 | 关键约束 |
|------|------|----------|
| **Python 3** (stdlib) | SVG 生成、Pillow 叠加文字 | `Pillow`, `subprocess`, `json`, `base64` |
| **Inkscape 1.4+** | SVG → PNG 栅格化 | 必须显式像素尺寸 |
| **curl** | Agnes API 调用 | 替代 Python requests（SSL 故障） |
| **Agnes API** | AI 图片生成 | `agnes-image-2.1-flash`，仅支持1024×1024 |
| **Pillow** | 在 AI 底图上叠加中文文字 | 字体 `/System/Library/Fonts/STHeiti Medium.ttc` |
| **opencli** | 浏览器自动化发布小红书草稿 | `--draft true`, `--window foreground`, `--site-session persistent` |
| **lark-cli** | 飞书文档管理（预览用） | `+create`, `+media-insert`, `+update --mode overwrite` |
| **Git** | 版本控制 | push 手动触发 |

> `opencli gemini image` 不可靠，不用。

## 三、两条图片生成路线

### 路线 A：AI Photorealistic + Pillow 加字（推荐）

适用于旅行/赛事/人物类内容，视觉冲击力强。

```
写 article.md → 写 6 个 prompt（NO text）→ 生成 cover（无 ref）
  → 生成 cards 2-6（cover 为 ref）→ Pillow 叠中文 + 半透明底条
  → 飞书文档 → 用户审核 → 小红书草稿
```

#### Prompt 模版
```markdown
[场景描述], photorealistic, high detail, sports/nature photography style,
NO text or labels or words anywhere in the image
```

**关键规则**：
- 禁止色号代码（`#FAF7F2` → 写 `cream colored`）
- 禁止要求 AI 渲染中文（必然乱码）
- 人种必须显式指定（见 MEMORY.md 人种对应表）
- 内容安全敏感词：`colonial` `flag` `drape` `riot` `poor` `slum` → 用到就 400

#### 封面生成（无 ref）
```bash
python3 -c "import json; json.dump({'model':'agnes-image-2.1-flash','prompt':'...',...}, open('/tmp/payload.json','w'))"
curl -s --max-time 300 -X POST "https://apihub.agnes-ai.com/v1/images/generations" \
  -H "Authorization: Bearer $KEY" -H "Content-Type: application/json" \
  -d @/tmp/payload.json -o /tmp/response.json
URL=$(python3 -c "import json; print(json.load(open('/tmp/response.json'))['data'][0]['url'])")
curl -sL --max-time 60 "$URL" -o 01-cover.png
```

#### 内容卡生成（以 cover 为 ref）
```bash
python3 -c "
import json, base64
with open('01-cover.png','rb') as f: b64=base64.b64encode(f.read()).decode()
json.dump({'model':'agnes-image-2.1-flash','prompt':'...','n':1,'size':'1024x1024',
  'image':'data:image/png;base64,'+b64}, open('/tmp/payload.json','w'))
"
curl -s --max-time 300 ... -d @/tmp/payload.json ...
```

#### Pillow 叠加中文
```python
from PIL import Image, ImageDraw, ImageFont
img = Image.open("raw.png").convert("RGBA")
W, H = img.size
overlay = Image.new("RGBA", (W, H), (0, 0, 0, 0))
draw_bg = ImageDraw.Draw(overlay)
draw_bg.rectangle([0, H-140, W, H], fill=(0, 0, 0, 160))
img = Image.alpha_composite(img, overlay)
draw = ImageDraw.Draw(img)
font = ImageFont.truetype("/System/Library/Fonts/STHeiti Medium.ttc", 36)
```
文本居中：`textbbox` 计算宽度 → `(W - tw) // 2`

### 路线 B：SVG + Inkscape（仅用于数据/排行榜）

适用于知识科普、数据盘点、赛制说明类内容。

```
写 article.md → 写 gen_cards.py（SVG template）→ python3 gen_cards.py
  → Inkscape 自动转 PNG → 飞书文档 → 用户审核 → 小红书草稿
```

数据类卡片需加视觉元素（色条、图标、大数字），纯文字+横条用户反馈"不吸引人"。

## 四、目录规范

```
<topic>-xhs/
├── article.md          # 标题首行 + 正文 + 话题标签
├── gen_cards.py        # 路线B专用（SVG模板）
├── output/             # 路线B输出（SVG+PNG）
├── image-cards/
│   └── <slug>/
│       ├── prompts/    # 6个prompt文件
│       ├── 01-cover.png
│       ├── 02-xxx.png
│       └── ...
│       └── overlay_text.py  # Pillow 加字脚本（SCRIPT_DIR=src, dirname(dirname)=输出目录）
└── README.md           # 可选
```

## 五、内容约束

| 约束 | 值 |
|------|-----|
| 标题 | ≤ 20 字符（UTF-8 字符数） |
| 正文 | ≤ 950 字 |
| 正文首行 = 标题 | 用户偏好 |
| 正文不含时间/版本号 | 去掉日期、版本号 |
| 图片 | ≤ 9 张/帖，通常 6 张 |
| 话题标签 | 正文末尾 `#话题`，不传 `--topics` |
| 正文格式 | 纯文本，无 Markdown |
| 世界杯活动 | 必须带 `#世界杯里看世界` `#小众国家游记` |

## 六、飞书文档操作

```bash
# 创建文档
lark-cli docs +create --title "标题（审核稿）" --markdown "..." --as user

# 替换内容 + 图片（必须先 overwrite 再重新 insert）
lark-cli docs +update --doc $DOC_ID --mode overwrite --markdown "..." --as user
lark-cli docs +media-insert --doc $DOC_ID --file ./xxx.png --as user

# 不支持局部替换图片，只能全量 overwrite
# ⚠️ `--mode overwrite --markdown "..."` 报错需要 `--command`，方案：直接新建文档重插
```

## 七、小红书草稿

```bash
opencli xiaohongshu publish "$(cat article.md)" \
  --title "标题（≤20字）" \
  --images "img1.png,img2.png,..." \
  --window foreground --site-session persistent --draft true -f yaml
```

## 八、Agnes API 故障排查

| 故障 | HTTP | 修复 |
|------|------|------|
| `content_policy_violation` | 400 | 简化 prompt，去掉敏感词 |
| `upstream error` | 500 | 重试 1-3 次 |
| SSL 握手失败 | - | 用 curl 替代 Python requests |
| 图片截断 | 200但损坏 | 重新下载 |
| 中文乱码 | - | Pillow 叠加，不让 AI 写中文 |

## 九、已知踩坑

### 人种必须显式指定
AI 默认出东亚面孔。完整人种对应表见 `MEMORY.md`。

### Agnes API 不稳定的关键词
`colonial`, `flag`（某些语境）, `drape`, `poor`, `slum`, `refugee`, `conflict`, `war`（非足球语境）

### 多轮修复原则
- 用户只抱怨某张图 → 只修那张
- 改 prompt 后要全部重生成 → 先问用户确认
- 备份原始 prompt 到 `prompts/`

### 图片风格决策
- 用户说"不吸引人" → 切换到另一条路线
- 不要在同一路线上反复硬磨
- 旅行/比赛用 AI photorealistic，数据用 SVG

## 十、参考文献
- `AGENTS.md` — 硬规则入口
- `MEMORY.md` — 案例记录 + 踩坑沉淀
- `twitter生产流水线大纲.md` — Twitter 发布规范
- `今日头条生产流水线大纲.md` — 头条发布规范